spring.application.name=StudentManagementSystem
# PostgreSQL Database Ba?lant?s?
spring.datasource.url=jdbc:postgresql://localhost:5432/sms
spring.datasource.username=postgres
spring.datasource.password=12345
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Ayarlar? (PostgreSQL)
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# Hibernate DDL (Veritaban? ?emas? Y�netimi)
spring.jpa.hibernate.ddl-auto=update

# SQL Sorgular?n? Konsolda G�sterme (Opsiyonel)
spring.jpa.show-sql=true
logging.level.org.hibernate.SQL=DEBUG

server.port=8085 